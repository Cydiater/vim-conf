#include <bits/stdc++.h>

template<typename T> inline T max(T a, T b) {
	return a > b ? a : b;
}
template<typename T> inline T min(T a, T b) {
	return a < b ? a : b;
}
template<typename T> inline bool cmax(T &a, T b) {
	return a < b ? a = b, 1 : 0;
}
template<typename T> inline bool cmin(T &a, T b) {
	return a > b ? a = b, 1 : 0;
}

int main() {

#ifdef cydiater_io

	freopen("in", "r", stdin);
	freopen("out", "w", stdout);

#endif

	return 0;

}

